@model LoginViewModel
<form asp-controller="Members" asp-action="Login">
    <div class="container" style="width:50%">
        <div class="form-group">
            <label asp-for="Account" class="form-label"></label>
            <input type="text" asp-for="Account" class="form-control"></input>
            <span asp-validation-for="Account" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Password" class="form-label"></label>
            <input type="text" asp-for="Password" class="form-control"></input>
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>
        <div class="form-group">
            <button type="submit" class="btn btn-primary">送出</button></br>
            <span>@ViewBag.failLogin</span>
        </div>
    </div>
</form>
@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}