@page
@model LogoutModel
@{
    ViewData["Title"] = "登出";
}

<header>
    <h1>@ViewData["Title"]</h1>
    @{
        if (User.Identity?.IsAuthenticated ?? false)
        {
            <form class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/", new { area = "" })" method="post">
                <button type="submit" class="nav-link btn btn-link text-dark">登出t</button>
            </form>
        }
        else
        {
            <p>您已成功登出。</p>
        }
    }
</header>
@section Scripts
{
    document.getElementById("")sessionStorage.clear();
}